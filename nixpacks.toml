[variables]
NPM_CONFIG_PRODUCTION = 'false'

[phases.setup]
nixPkgs = ['nodejs_20', 'npm-9_x']

[phases.install]
cmds = ['npm run install:all']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'